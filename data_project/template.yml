files_to_render:
  - snowflake.yml
variables:
  - name: name
    default: my_project
    prompt: "Name of the PROJECT object to be created in Snowflake"
    type: string
  - name: stage
    default: my_project_stage
    prompt: "Target STAGE to where project files should be deployed"
    type: string
