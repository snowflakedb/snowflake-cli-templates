files_to_render:
  - snowflake.yml
variables:
  - name: project_name
    promt: "Name of the snowpatk project"
    default: my_snowpark_project
    type: string
  - name: stage
    prompt: "What stage should the procedures and functions be deployed to?"
    default: dev_deployment
    type: string